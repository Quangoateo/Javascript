var pi_num = 3.14;
function calc_area(radius)
{
    console.log(pi_num* radius * radius);
    if(true)
    {
        var notification = '(area of circle)';

    }
    //access the declared variable 
    console.log (notification);


}

calc_area(5);